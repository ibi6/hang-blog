# Netlify 构建与部署配置
[build]
  command = "npm run build"
  publish = "dist"

# 确保 SPA 路由回退不会吃掉 /admin
[[redirects]]
  from = "/admin/*"
  to = "/admin/:splat"
  status = 200
  force = false

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
